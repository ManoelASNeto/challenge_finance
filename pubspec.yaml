name: challenge_finance
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  bloc: ^8.1.1
  connectivity_plus: ^3.0.4
  charts_flutter_new: ^0.12.0
  dartz: ^0.10.1
  equatable: ^2.0.5
  fl_chart: ^0.62.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.2
  get_it: ^7.2.0
  http: ^0.13.5
  http_interceptor: ^1.0.2
  d_chart: ^2.2.11
 


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
